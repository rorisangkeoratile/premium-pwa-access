# Copy to .env.local for local development. On Render, set the same names under Environment.
# Only names starting with VITE_ are sent to the browser, so never put a secret here.

# Base URL of the FastAPI backend, with no trailing slash.
VITE_API_URL=http://localhost:8000
